node {

    stage('Checkout') {
        checkout scm
    }

    stage('Build'){
        sh "mvn package"
    }
    stage('deploy'){
        sh "cd ansible && ansible-playbook -i inventory playbook.yml" 
        }
}
